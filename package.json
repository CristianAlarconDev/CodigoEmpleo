{
  "name": "codigo-empleo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server" : "json-server --watch db.json --port 8080"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  },
  "dependencies": {
    "bootstrap": "^5.3.8",
    "json-server": "^1.0.0-beta.3"
  }
}
